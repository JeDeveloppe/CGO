<form method="post" action="{{ path('app_cgo_delete', {'id': cgo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cgo.id) }}">
    <button class="btn">Delete</button>
</form>
