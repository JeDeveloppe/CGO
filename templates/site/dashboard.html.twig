{% extends 'site/base.html.twig' %}

{% block title %}Tableau de bord{% endblock %}

{% block body %}
    
    <h1 class="col-12 text-center my-5 h1">Tableau de bord</h1>

    <div class="row justify-content-around">
        <div class="col-4 d-flex justify-content-center align-items-center p-5">
            <a class="btn btn-lg btn-primary" href="{{ path('app_site_calcul_distance') }}">Calculs distances</a>
        </div>
        <div class="col-4 d-flex justify-content-center align-items-center p-5">
            <a class="btn btn-lg btn-primary disabled" href="#">A venir...</a>
        </div>
        <div class="col-4 d-flex justify-content-center align-items-center p-5">
            <a class="btn btn-lg btn-primary disabled" href="#">A venir...</a>
        </div>
    </div>
    <div class="row justify-content-around border-top border-primary">
        <div class="col-12 h3 mt-5">Gestion du CGO:</div>
        <div class="col-4 d-flex justify-content-center align-items-center p-5">
            <a class="btn btn-lg btn-outline-dark" href="{{ path('app_departement_index') }}">Gérer les départements</a>
        </div>
        <div class="col-4 d-flex justify-content-center align-items-center p-5">
            <a class="btn btn-lg btn-outline-dark" href="{{ path('app_shop_index') }}">Gérer les centres</a>
        </div>
        <div class="col-4 d-flex justify-content-center align-items-center p-5">
            <a class="btn btn-lg btn-primary disabled" href="#">A venir...</a>
        </div>
    </div>
  

{% endblock %}
